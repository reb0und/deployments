---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: traefik
  namespace: kube-system
spec:
  chart:
    chartRef:
      kind: OCIRepository
      name: traefik
      namespace: flux-system
  interval: 10m0s
