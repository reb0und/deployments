---
apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TCPRoute
metadata:
  name: mc-route
  namespace: kube-system
spec:
  parentRefs:
    - name: traefik
      sectionName: mc-gw
  rules:
    - backendRefs:
      - name: minecraft
        port: 25565
