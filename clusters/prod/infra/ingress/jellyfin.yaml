---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: jellyfin-route
  namespace: kube-system
spec:
  parentRefs:
    - name: traefik
      sectionName: web-gw
  rules:
    - backendRefs:
      - name: jellyfin
        namespace: default
        port: 8096
