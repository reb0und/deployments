---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: jellyfin-route
  namespace: kube-system
spec:
  parentRefs:
    - name: web-gw
      sectionName: web
  rules:
    - backendRefs:
      - name: jellyfin
        port: 80
