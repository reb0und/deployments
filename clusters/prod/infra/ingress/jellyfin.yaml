---
apiVersion: gateway.networking.k8s.io/v1alpha2
kind: HTTPRoute
metadata:
  name: jellyfin-route
  namespace: kube-system
spec:
  parentRefs:
    - name: web-gw
      sectionName: web
  rules:
    - backendRefs:
      - name: jellyfin
        port: 80
